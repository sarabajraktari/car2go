{% extends "base.twig" %}

{% block content %}
    <div class="w-full">
           {% if not is_single_car_page %}
            {{ include("modules.twig") }}
        {% endif %}
    </div>
          {% if is_single_car_page %}
             {{ include("templates/single-car.twig", { car: car }) }} 
                {{ include("modules.twig") }}
        {% endif %} 

        {% if is_author_page %}
           {{ include("templates/author.twig", { author: author  }) }}
        {% endif %}  

        {% if show_search_bar %}
             {% include 'templates/SearchBar.twig' with {'brands': brands, 'cities': cities} %}
        {% endif %} 
        

{% endblock %}